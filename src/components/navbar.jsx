import React, { useState } from 'react'

const Navbar = () => {
  const [ navLink, setNavLink ] = useState('home');
  return (
    <div style={{
        width: "100%",
        height: "50px"
    }}>
        
    </div>
  )
}

export default Navbar